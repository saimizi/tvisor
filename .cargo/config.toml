[build]
target = "aarch64-unknown-none"

[target.aarch64-unknown-none]
rustflags = ["-C", "link-arg=-Tscripts/rpi.ld"]

# Run host-side unit tests: the bare-metal target has no Rust test harness.
[alias]
test-host = "test --lib --target x86_64-unknown-linux-gnu"
